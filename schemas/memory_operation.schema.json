{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "MemoryOperation",
  "type": "object",
  "required": [
    "id",
    "operation",
    "status",
    "actor_id",
    "created_at"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Stable identifier for this portable OACS record."
    },
    "operation": {
      "type": "string",
      "enum": [
        "memory.observe",
        "memory.propose",
        "memory.commit",
        "memory.query",
        "memory.read",
        "memory.correct",
        "memory.deprecate",
        "memory.supersede",
        "memory.forget",
        "memory.blur",
        "memory.sharpen",
        "memory.audit",
        "memory.export",
        "memory.import"
      ],
      "description": "Standard operation name represented by this envelope."
    },
    "status": {
      "type": "string",
      "enum": [
        "requested",
        "completed",
        "denied",
        "failed"
      ],
      "description": "Lifecycle status for this record in the portable OACS model."
    },
    "actor_id": {
      "type": [
        "string",
        "null"
      ],
      "description": "Optional actor identifier associated with this operation or projection."
    },
    "target_id": {
      "type": [
        "string",
        "null"
      ],
      "description": "Optional MemoryRecord identifier targeted by this operation."
    },
    "scope": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Portable scope labels that constrain where the record applies."
    },
    "namespace": {
      "type": "string",
      "description": "Logical namespace used to separate records without requiring a specific storage backend."
    },
    "depth": {
      "type": [
        "integer",
        "null"
      ],
      "minimum": 0,
      "maximum": 5,
      "description": "Optional memory depth involved in this operation."
    },
    "arguments": {
      "type": "object",
      "description": "Portable operation arguments; implementations may map them to local APIs."
    },
    "result": {
      "type": "object",
      "description": "Portable operation result envelope; implementations may include adapter-neutral details."
    },
    "audit_event_id": {
      "type": [
        "string",
        "null"
      ],
      "description": "AuditEvent identifier associated with this operation."
    },
    "created_at": {
      "type": "string",
      "description": "Creation timestamp encoded as a portable string."
    }
  },
  "additionalProperties": false
}
