{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "MemoryCall",
  "type": "object",
  "required": [
    "id",
    "op",
    "status",
    "arguments",
    "result"
  ],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^mcall_[A-Za-z0-9_]+$",
      "description": "Stable identifier for this portable OACS record."
    },
    "op": {
      "type": "string",
      "enum": [
        "memory.query",
        "memory.query.deepen",
        "memory.read",
        "memory.extract_evidence"
      ],
      "description": "Standard memory-call operation discriminator."
    },
    "status": {
      "type": "string",
      "enum": [
        "completed",
        "denied",
        "failed"
      ],
      "description": "Lifecycle status for this record in the portable OACS model."
    },
    "arguments": {
      "type": "object",
      "description": "Portable operation arguments; implementations may map them to local APIs."
    },
    "result": {
      "type": "object",
      "description": "Portable operation result envelope; implementations may include adapter-neutral details."
    }
  },
  "additionalProperties": false
}
